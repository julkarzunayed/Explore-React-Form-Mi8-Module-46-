/**
 * 1. event.target.[name of the input field].value;
 * 2. using form action and the formData in the action handler. formData.get("[name of the input field]");
 * 3. controlled component. use state in change of the field. useful to dynamically handle error.
 * 
 * 4. Uncontrolled  using useRef() hook
 * 
 */